import '../globals.css';

export const metadata = {
  title: 'Account Pending Approval - ET AI Lab Portal',
  description: 'Your account registration is pending approval',
};

export default function PendingApprovalLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
} 